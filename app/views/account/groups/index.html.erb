<div class="col-md-12">

  <h2 class="text-center"> 我收藏的影片 </h2>

  <table class="table">
    <thead>
      <tr>
        <th> # </th>
        <th> 电影名称 </th>
        <th> 内容 </th>
        <th> 评论条数 </th>
        <th> 最后更新 </th>
      </tr>
    </thead>

    <tbody>
      <% @groups.each do |group| %>
        <tr>
          <td> # </td>
          <td> <%= link_to(group.title, group_path(group)) %> </td>
          <td> <%= group.description %> </td>
          <td> <%= group.posts.count %> </td>
          <td> <%= group.updated_at %> </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
